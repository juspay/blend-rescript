node_modules/
lib/
.bs/
.rescript/
.bsb.lock
.env
.DS_Store
*.bs.js
*.cjs
*.tgz

# Note: src/*.js (ReScript-emitted output) is committed for PR review visibility.
# It is NOT shipped to consumers — the "files" allowlist in package.json ships
# only .res/.resi sources, and consumers compile via their own rescript install.
