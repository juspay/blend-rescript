// SCAFFOLD generated by scripts/scaffold-figma-component-maps.mjs from src/RadioV2Content.res.
// ReScript-side prop names/kinds (and, for simple enums, code-side constructor
// names) are derived from our own bindings -- safe, no Figma guessing.
// Figma-side property names and enum KEYS are UNKNOWN and must be filled in
// by checking the real component in Figma Desktop. This file is NOT ready
// for figma:generate until every 'null'/'TODO_FIGMA_KEY' below is resolved
// (unresolved entries are skipped by the codegen script, not guessed).

export default {
  figmaComponentName: null, // TODO: confirm the Figma component/component-set name
  codeComponent: 'RadioV2Content',
  id: 'radioV2Content',
  imports: ['open JuspayRescriptBlend'],
  props: {
    uniqueId: { figmaProp: null /* TODO */, kind: 'string' },
    disabled: { figmaProp: null /* TODO */, kind: 'bool' },
    error: { figmaProp: null /* TODO */, kind: 'bool' },
    required: { figmaProp: null /* TODO */, kind: 'bool' },
    size: {
      figmaProp: null, // TODO: Figma property name
      kind: 'enum',
      values: {
        // TODO_FIGMA_KEY: 'SelectorV2Types.Sm', // ReScript: Sm (bound value "sm")
        // TODO_FIGMA_KEY: 'SelectorV2Types.Md', // ReScript: Md (bound value "md")
        // TODO_FIGMA_KEY: 'SelectorV2Types.Lg', // ReScript: Lg (bound value "lg")
      },
    },
    label: { figmaProp: null /* TODO */, kind: 'string' },
    subLabel: { figmaProp: null /* TODO */, kind: 'string' },
    // TODO (unsupported): slot -- SelectorV2Types.selectorV2SlotConfig -- variant has payload(s) or is a record, not a simple enum
    // TODO (unsupported): tokens -- SelectorV2Types.radioV2TokensType -- variant has payload(s) or is a record, not a simple enum
    // TODO (unsupported): labelMaxLength -- float -- not confidently classifiable
    // TODO (unsupported): subLabelMaxLength -- float -- not confidently classifiable
    subLabelId: { figmaProp: null /* TODO */, kind: 'string' },
  },
}
